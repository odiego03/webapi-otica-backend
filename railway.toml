[build]
builder = "NIXPACKS"
buildCommand = "dotnet publish -c Release -o ./publish"

[build.environment]
DOTNET_VERSION = "8.0"  # ← ESTA LINHA É CRUCIAL!
ASPNETCORE_VERSION = "8.0"

[deploy]
numReplicas = 1
startCommand = "dotnet ./publish/WebApi-otica.dll"
restartPolicyType = "ON_FAILURE"